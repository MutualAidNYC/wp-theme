name: Assigns a milestone

on:
  pull_request:
    types:
      - opened
  issues:
    types:
      - opened
  push:
    branches: [ add/auto-assign-milestone ]

jobs:
  open:
    runs-on: ubuntu-latest

    steps:
      - name: Get latest milestone data
        run: bash .github/get-milestone.sh

      - name: Assigns milestone
        if: ${{ github.event.issue.id }}
        run: bash .github/assign-milestone.sh ${{ github.event.issue.id }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
